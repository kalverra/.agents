name: Prompt Evaluation

on:
  pull_request:
    branches:
      - main
    paths:
      - "*.md"
      - "scripts/eval/**"
      - ".github/workflows/eval-pr.yaml"

jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout PR code
        uses: actions/checkout@v6
        with:
          path: "pr-repo"

      - name: Checkout Main code
        uses: actions/checkout@v6
        with:
          ref: "main"
          path: "main-repo"

      - name: Compare prompt evaluations
        run: |
          cd pr-repo/cmd/agents-cli && go build -o ../../../agents-cli . && cd ../../..
          ./agents-cli ci-compare \
            main-repo/scripts/eval/eval_history.json \
            pr-repo/scripts/eval/eval_history.json \
            results.md

      - name: Post check results
        if: always()
        run: |
          cat results.md >> $GITHUB_STEP_SUMMARY
